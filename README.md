# testingTutorial

In the `src` folder can be found some classes which model a simple restaurant system.

The task is to adequately and cleanly test this system using Python's testing suite.

The architecture of this system is such that it would be wise to using Python's mocking library to conduct much of the
testing, in order to ensure that only one piece of functionality is being tested at a time
